[pytest]
norecursedirs = .Ctl data gen .tox

[tox]
envlist =
   py{27,34,35,36}

[testenv]
passenv = CI TRAVIS*
setenv =
  PYTHONWRITEBYTECODE=1
  PYTHONWARNINGS=once

deps =-rCtl/requirements-test.txt

commands = py.test -vs --cov="graphsrv" --cov-report term-missing
